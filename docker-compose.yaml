services:

  database_instance:
    image: mysql:latest
    container_name: javadao_db
    volumes:
      - db_volume_name:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
    ports:
      - 3306:3306

volumes:
  db_volume_name:
    external: false